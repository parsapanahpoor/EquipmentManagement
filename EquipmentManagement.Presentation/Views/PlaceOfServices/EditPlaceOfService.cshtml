@model EquipmentManagement.Domain.DTO.SiteSide.PlaceOfService.EditPlaceOfServiceDTO

@{
    ViewData["Title"] = "ویرایش مکان جدید";
}

@section Scripts
{
    <partial name="_JqueryValidation" />
}

@section Breadcrumbs
{
    <li>
        <a asp-area="" asp-controller="PlaceOfServices" asp-action="FilterPlaceOfServices"> مدیریت مکان ها  </a>
        <a href="javascript:void(0)"> @ViewData["Title"] </a>
    </li>
}

<form method="post" asp-action="EditPlaceOfService" asp-controller="PlaceOfServices" asp-area="" enctype="multipart/form-data">
    <input asp-for="ParentId" type="hidden" />
    <input asp-for="PlaceOfServiceId" type="hidden" />
    <div class="row">
        <div class="col-lg-12 col-sm-12">
            <div class="widget">
                <div class="widget-header bordered-bottom bordered-info">
                    <span class="widget-caption"> @ViewData["Title"] </span>
                </div>
                <div class="widget-body">
                    <div>

                        <div class="row">
                            <div class="form-group col-md-12 col-sm-12">
                                <label asp-for="PlaceOfServiceTitle">عنوان</label>
                                <input type="text" class="form-control" asp-for="PlaceOfServiceTitle">
                                <span class="text-danger" asp-validation-for="PlaceOfServiceTitle"></span>
                            </div>

                        </div>

                        <hr class="margin-bottom-10 margin-top-10" />

                        <div class="row">
                            <div class="col-md-12 flex-style">
                                <button type="submit" class="btn btn-labeled btn-info">
                                    <i class="btn-label glyphicon glyphicon-ok"></i>ثبت
                                </button>
                            </div>
                        </div>

                    </div>
                </div>
            </div>
        </div>

    </div>
</form>

